/**
 * DECOMMISSIONED: Generate Letters
 * This stub intentionally disables the legacy generator.
 * New flow: use `letters.generate.phase1`.
 */

const msg = "Legacy 'Generate Letters' disabled. Use letters.generate.phase1 instead.";

function noopResponse() {
  return { ok: false, disabled: true, reason: msg };
}

// Try to cover common export styles used in Base44
exports["Generate Letters"] = async (_args, _ctx) => noopResponse();
exports["letters.generate"]  = async (_args, _ctx) => noopResponse();

// Fallbacks (in case this function used default/module export patterns)
module.exports = async (_args, _ctx) => noopResponse();
exports.default = async (_args, _ctx) => noopResponse();
